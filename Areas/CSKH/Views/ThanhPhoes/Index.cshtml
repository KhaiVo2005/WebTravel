@model IEnumerable<ThanhPho>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/CSKH/Views/Shared/_LayoutCSKH.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenThanhPho)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenThanhPho)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaTP">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaTP">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaTP">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
